import { ScrollToTheTopDirective } from './scroll-to-the-top.directive';

describe('ScrollToTheTopDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToTheTopDirective();
    expect(directive).toBeTruthy();
  });
});
